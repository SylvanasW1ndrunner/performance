/* 主内容区域样式 - 与固定宽度250px的侧边栏配合 */
.main-content {
    margin-left: 250px; /* 与侧边栏宽度相同 */
    width: calc(100% - 250px); /* 宽度为视口宽度减去侧边栏宽度 */
    padding: 30px;
    background-color: #fff;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}

/* 内容标题区域 */
.content-header {
    margin-bottom: 25px;
}

.content-header h1 {
    font-size: 24px;
    color: #333;
    margin-bottom: 8px;
}

.content-header p {
    color: #666;
    font-size: 14px;
}

/* 搜索筛选区域 */
.search-section {
    background-color: #f9f9f9;
    border-radius: 8px;
    padding: 20px;
    margin-bottom: 25px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.05);
}

.search-row {
    display: flex;
    flex-wrap: wrap;
    margin-bottom: 15px;
}

.search-row:last-child {
    margin-bottom: 0;
}

.search-item {
    flex: 1;
    min-width: 200px;
    margin-right: 20px;
    margin-bottom: 10px;
}

.search-item:last-child {
    margin-right: 0;
}

.search-item label {
    display: block;
    margin-bottom: 5px;
    font-size: 14px;
    color: #555;
}

.search-item input, .search-item select {
    width: 100%;
    padding: 8px 12px;
    border: 1px solid #ddd;
    border-radius: 4px;
    font-size: 14px;
    color: #333;
}

.search-item select {
    background-color: white;
    cursor: pointer;
}

.search-button {
    display: flex;
    align-items: flex-end;
    margin-bottom: 10px;
}

#search-btn, #reset-btn {
    padding: 8px 16px;
    border: none;
    border-radius: 4px;
    font-size: 14px;
    cursor: pointer;
    transition: background-color 0.2s;
}

#search-btn {
    background-color: #3498db; /* 与侧边栏active颜色匹配 */
    color: white;
    margin-right: 10px;
}

#search-btn:hover {
    background-color: #2980b9;
}

#reset-btn {
    background-color: #f0f0f0;
    color: #666;
}

#reset-btn:hover {
    background-color: #e0e0e0;
}

/* 表格容器 */
.table-container {
    flex: 1;
    overflow: auto;
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    max-height: calc(100vh - 250px); /* 限制最大高度，确保在视口内 */
}

.employee-table {
    width: 100%;
    border-collapse: collapse;
}

.employee-table thead {
    background-color: #f5f7fa;
    position: sticky;
    top: 0;
    z-index: 10;
}

.employee-table th {
    padding: 15px;
    text-align: left;
    font-weight: 600;
    color: #444;
    border-bottom: 1px solid #eee;
}

.employee-table td {
    padding: 15px;
    border-bottom: 1px solid #eee;
    color: #555;
}

.employee-table tbody tr:hover {
    background-color: #f9f9f9;
}

.employee-table tbody tr:last-child td {
    border-bottom: none;
}

/* 评分按钮 */
.score-btn {
    padding: 6px 12px;
    background-color: #3498db; /* 与侧边栏active颜色匹配 */
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: background-color 0.2s;
}

.score-btn:hover {
    background-color: #2980b9;
}

/* 自定义滚动条样式 */
.table-container::-webkit-scrollbar {
    width: 8px;
    height: 8px;
}

.table-container::-webkit-scrollbar-track {
    background: #f1f1f1;
    border-radius: 4px;
}

.table-container::-webkit-scrollbar-thumb {
    background: #ccc;
    border-radius: 4px;
}

.table-container::-webkit-scrollbar-thumb:hover {
    background: #aaa;
}